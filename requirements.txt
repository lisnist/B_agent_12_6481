# Full Python environment for backend, tools, memory, MCP search, Qwen API proxy,
# and optional local Transformers inference.
# Tested with Python 3.10 in /data/shixun/Anaconda3/envs/agent.
# Torch uses the CUDA 11.8 wheel build used by the server validation.
--extra-index-url https://download.pytorch.org/whl/cu118

# Core config and data handling.
PyYAML==6.0.3
numpy==2.2.6
Pillow>=11,<13
pydantic>=2.11,<3
requests==2.34.2

# Optional local model backend.
transformers==5.12.1
accelerate==1.14.0
sentencepiece==0.2.1
safetensors==0.8.0
tokenizers==0.22.2
huggingface_hub==1.20.1
filelock==3.29.4
fsspec==2026.2.0
Jinja2==3.1.6
packaging==26.2
regex==2026.5.9
tqdm==4.68.3
fastapi
uvicorn
paramiko
ddgs>=9.14,<10
langchain-openai>=1.0,<2
